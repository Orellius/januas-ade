//! Januas ADE — binary entry point.
//!
//! v0.0.0 scaffold. Slice S0 ships a green build + CI floor; the real entry
//! point lands in S1 once the window + GPU surface are wired.

fn main() {
    println!("Januas ADE v0.0.0 — scaffold");
}
